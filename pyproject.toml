[project]
name = "mediguide"
version = "0.1.0"
description = "AI-powered medical assistant using FastAPI, LangChain, and ChromaDB for RAG-based healthcare Q&A"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
]
